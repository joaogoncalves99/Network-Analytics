library(data.table)
library(igraph)
library(ggplot2)
library(shiny)


ui <- fluidPage(
  # Application title
  titlePanel("Network of Instagram Influencers"),
  
  # Sidebar 
  sidebarLayout(
    sidebarPanel(
      h2("Hello")
      
    )
  )
)

ui



